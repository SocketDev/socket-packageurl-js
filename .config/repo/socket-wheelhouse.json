{
  "$schema": "./socket-wheelhouse-schema.json",
  "schemaVersion": 1,
  "repoName": "socket-packageurl-js",
  "repo": {
    "type": "solo"
  },
  "build": {
    "from": "npm-registry",
    "type": "js"
  },
  "docs": {
    "llmsTxt": true
  },
  "release": {
    "provenanceOrphanBaseline": [
      {
        "id": "@socketregistry/packageurl-js@1.5.0",
        "reason": "Attested commit daac6c59 carries the right 1.5.0 manifest, but v1.5.0 tags a different commit (2b810e49) and a re-pack at the attested tree DIVERGES from the published tarball — the built dist/ bytes differ, so the build is not reproducible and no tag can be proven to mark the published content."
      }
    ]
  }
}
